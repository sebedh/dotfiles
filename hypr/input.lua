hl.config({
  input = {
    kb_layout = "us,se",
    kb_options = "grp:win_space_toggle,caps:escape,grp:shift_caps_toggle",

    repeat_rate = 50,
    repeat_delay = 300,

    numlock_by_default = true,

    touchpad = {
      scroll_factor = 0.4,
    },
  },
})
